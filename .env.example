# Copy this file to ".env" at the project root and adjust if needed.
# These values are consumed by docker-compose.yml, scripts/load_raw.py and
# dbt/profiles.yml (via env_var()).

POSTGRES_USER=dwh_user
POSTGRES_PASSWORD=dwh_password
POSTGRES_DB=ecommerce_dwh
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
